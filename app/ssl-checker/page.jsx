import SslChecker from "@/components/tools/SslChecker";

export const metadata = {
  title: "EaseMyTools - Ssl Checker",
  description: "Use the Ssl Checker tool on EaseMyTools.",
};

export default function Page() {
  return <SslChecker />;
}
